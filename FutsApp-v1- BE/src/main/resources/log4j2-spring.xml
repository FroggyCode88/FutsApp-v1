<?xml version="1.0" encoding="UTF-8"?>
<Configuration name="properties_configuration">

    <Appenders>
        <!-- ConsoleAppender will print logs on console -->
        <Console name="consoleLogger" target="SYSTEM_OUT" follow="true">
            <PatternLayout disableAnsi="false" pattern="%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}} %highlight{${LOG_LEVEL_PATTERN:-%5p}}{FATAL=red blink, ERROR=red, WARN=yellow bold, INFO=green, DEBUG=green bold, TRACE=blue} %style{${sys:PID}}{magenta} [%15.15t] %style{%-40.40C{1.}}{cyan} : %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>
        </Console>

        <!-- RollingFileAppender will print logs in file which can be rotated based on time or size -->
        <RollingFile name="fileLogger"
                     fileName="C:/logs/futsApp/futsApp.log"
                     filePattern="C:/logs/futsApp/futsApp%d{yyyy-MM-dd}.log.gz">
            <PatternLayout pattern= "%d{ISO8601} [%t] %-5p %c - %m%n"  />

            <Policies>
                <!-- Rotate log file each day and keep 60 days worth -->
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
            <DefaultRolloverStrategy>
                <Delete basePath= "C:/logs/futsApp" maxDepth="1">
                    <!-- Delete files older than 60 days -->
                    <IfLastModified age="60d"/>
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>
    </Appenders>

    <!-- Configure root logger additivity="false"-->
    <Loggers>
        <Root level="info" >
            <AppenderRef ref="consoleLogger"/>
            <AppenderRef ref="fileLogger"/>
        </Root>
    </Loggers>
</Configuration>
